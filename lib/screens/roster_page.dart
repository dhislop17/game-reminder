import 'package:flutter/material.dart';


class RosterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
   return Scaffold(
     appBar: AppBar(
       title: Text("Roster"),
     ),
     body: Container(child: Text("Roster")),
   );
  }
}